meta:
  environment: python
  toolchain:
    name: pip
    version: "3.10"
build:
  requirementsPath: requirements.txt
run:
  command: uvicorn main:app
  persistenceMount: /data
  containerPort: "80"
